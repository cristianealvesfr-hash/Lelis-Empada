const puppeteer = require('puppeteer'); (async () = const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg = LOG:', msg.text())); page.on('pageerror', error = ERROR:', error.message)); await page.goto('http://localhost:5173'); await page.waitForSelector('.group.bg-white.rounded-2xl'); await page.click('.group.bg-white.rounded-2xl'); await page.waitForSelector('button'); const buttons = await page.('button'); for (const btn of buttons) { const text = await page.evaluate(el =, btn); if (text 
